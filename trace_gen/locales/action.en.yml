en:
  __question-prompt: <big>Build a program trace by clicking on the beginning <br> and end of statements in the order of execution.</big> <br>
  question-prompt: Press the actions of the algorithm in <br> the order they are evaluated. Activate actions <br> with play<i class="play small icon"></i> and stop<i class="stop small icon"></i> buttons. <br>
  program:   program
  algorithm: program
  sequence: sequence `%{name}`
  loop: loop `%{name}`
  loop-body: iteration of loop `%{loop_name}`
  selection:   selection `%{name}`
  alternative: selection `%{name}`
  if: IF branch with condition `%{cond_name}`
  else-if: ELSE-IF branch with condition `%{cond_name}`
  else: ELSE branch of selection `%{alt_name}`
  function: function `%{name}`
  function-body: body of function `%{name}`
  stmt: statement `%{name}`
  condition: condition `%{name}`
  expr:      condition `%{name}`
  return: return `%{name}`
  break: break
  continue: continue
